- file:
    path: ~/.dotfiles_backups/
    state: directory
    recurse: yes
  tags: dotfiles

- include: dotfiles.yml
  with_items:
    - vimrc
    - zshrc
    - gitignore_global
    - gitconfig
    - tmux.conf
    - npmrc
    - atom/config.cson
    - vim/colors/Tomorrow-Night-Eighties.vim
  tags: dotfiles

- name: Installing Antigen
  git: repo=https://github.com/zsh-users/antigen.git dest=~/.antigen clone=yes update=yes
  tags: dotfiles

- name: Change shell to ZSH
  user: shell=/bin/zsh user={{ item }}
  become: true
  become_method: sudo
  with_items:
    - root
    - gcaracuel

- include: pip.yml
  with_items:
    - virtualenv
    - virtualenvwrapper
    - ipython
    - thefuck
  tags: pip

- include: pip3.yml
  with_items:
    - asciinema
  tags: pip

- include: nvm.yml
- include: atom.yml
- include: vim.yml
- include: fonts.yml
- include: rvm.yml
