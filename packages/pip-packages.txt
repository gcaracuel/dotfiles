# pip-packages.txt — Python packages installed via mise (uv/python runtime)
# One package per line. Lines starting with # are comments.
# Edit this file to add/remove packages.
# Run: chezmoi apply  (triggers run_onchange script automatically)

jrnl    # Journal CLI for standups
