[user]
	email = gcaracuel@user.noreply.github.com
	name = gcaracuel
[includeIf "gitdir:~/Projects/work/gitlab"]
    path = ~/Projects/work/.gitconfig
[credential]
	helper = cache
[core]
	excludesfile = $HOME/.gitignore_global
	editor = vim
[push]
	default = simple
[mergetool]
	prompt = false
[merge]
	conflictstyle = diff3
	tool = vimdiff
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[pull]
	rebase = false
[init]
	defaultBranch = main
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
